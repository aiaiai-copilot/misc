{
  "name": "@misc-poc/infrastructure-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "sh -c 'if test -d src; then yarn exec tsc --build; else echo \"No src directory found, skipping build\"; fi'",
    "test": "jest",
    "test:fast": "jest --testNamePattern='^((?!\\[perf\\]).)*$'",
    "test:perf": "jest --testNamePattern='\\[perf\\]'",
    "lint": "sh -c 'if test -d src; then cd ../../../.. && yarn exec eslint packages/infrastructure/api/src --ext .ts; else echo \"No src directory found, skipping lint\"; fi'",
    "typecheck": "sh -c 'if test -d src; then cd ../../.. && yarn tsc --noEmit --project packages/infrastructure/api; else echo \"No src directory found, skipping typecheck\"; fi'",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@misc-poc/application": "workspace:*",
    "@misc-poc/domain": "workspace:*",
    "@misc-poc/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  }
}
